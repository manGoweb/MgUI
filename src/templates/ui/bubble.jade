include avatar.jade

mixin bubble(props = {})
	.mgui-bubble(class=cl([ props.isOur ? "is-our" : "is-their", props.avatarUrl && "has-avatar" ]))
		if props.text
			.mgui-bubble-text=props.text
		if props.imageUrl
			img.mgui-bubble-image(src=props.imageUrl,alt=props.imageUrl)
